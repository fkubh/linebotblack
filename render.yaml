services:
  - type: web
    name: linebotblack
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn lineborblack_cloud:app
    envVars:
      - key: CHANNEL_ACCESS_TOKEN
        sync: false
      - key: CHANNEL_SECRET
        sync: false
